{
  "name": "imgchecker",
  "private": true,
  "version": "0.0.1",
  "description": "image urls checker",
  "scripts": {
    "build": "stencil build",
    "start": "stencil build --dev --watch --serve",
    "test": "stencil test --spec --e2e",
    "test.watch": "stencil test --spec --e2e --watchAll",
    "generate": "stencil generate",
    "docs": "npm run build && rm -r docs && cp -a www docs"
  },
  "dependencies": {
    "@stencil/core": "^1.3.0",
    "@stencil/router": "^1.0.1",
    "bulma": "^0.7.5"
  },
  "license": "MIT",
  "devDependencies": {
    "@stencil/sass": "^1.0.1",
    "@types/jest": "24.0.18",
    "@types/puppeteer": "1.19.1",
    "jest": "24.8.0",
    "jest-cli": "24.8.0",
    "puppeteer": "1.19.0",
    "tslint": "^5.19.0",
    "workbox-build": "4.3.1"
  }
}
